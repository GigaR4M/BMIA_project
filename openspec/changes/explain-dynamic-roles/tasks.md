1. [x] Update `commands/role_commands.py` to add `explain_dynamic_roles` command.
   - [x] Define the descriptions for each dynamic role key (top_1, streamer, etc.).
   - [x] Implement the command to fetch role objects (to get mentions) and display criteria.
2. [ ] Validate the command by running the bot and invoking `/autorole explicar`.
